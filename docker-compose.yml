version: '3.8'

services:
  priceiq:
    build: .
    container_name: priceiq_app
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped